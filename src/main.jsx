import React from 'react';
import ReactDOM from 'react-dom/client';
import IMDFBuilder from './IMDFBuilder.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <IMDFBuilder />
  </React.StrictMode>
);
